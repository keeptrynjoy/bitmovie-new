<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.repository.movie.MovieRepository">
    <insert id="insertDetailData" parameterType="movie">
        INSERT INTO movie_tb (movie_pk,m_name,m_type,m_sdate,m_runtime,m_info) VALUES (#{movie_pk},#{m_name},#{m_type},#{m_sdate},#{m_runtime},#{m_info})
    </insert>
    <update id="updatePhoto" parameterType="Map">
        UPDATE movie_tb SET m_photo = #{m_photo} WHERE movie_pk = #{movie_pk}
    </update>

    <select id="selectMovieList">

    </select>
    <select id="selectMovieData" parameterType="String" resultType="movie">
        SELECT * FROM movie_tb WHERE movie_pk = #{movie_pk}
    </select>
</mapper>
