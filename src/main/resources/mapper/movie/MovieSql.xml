<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.repository.movie.MovieRepository">

    <!--  TMDB api 영화 정보 저장  -->
    <insert id="insertDetailData" parameterType="movie">
        INSERT INTO movie_tb (movie_pk, m_name, m_type, m_sdate, m_edate, m_runtime, m_info, m_country)
        VALUES (#{movie_pk}, #{m_name}, #{m_type}, #{m_sdate}, #{m_edate}, #{m_runtime}, #{m_info}, #{m_country});
    </insert>

    <!--  TMDB api 영화 정보 저장(사진, 영어제목, 동영상)  -->
    <update id="updateDataOne" parameterType="Map">
        UPDATE movie_tb
        SET ${column} = #{data}
        WHERE movie_pk = #{movie_pk}
    </update>

    <select id="selectMovieData" parameterType="String" resultType="movie">
        SELECT *
        FROM movie_tb
        WHERE movie_pk = #{movie_pk}
    </select>

    <select id="selectMovieYoN" parameterType="String" resultType="int">
        SELECT count(*)
        FROM movie_tb
        WHERE movie_pk = #{movie_pk}
    </select>


</mapper>
