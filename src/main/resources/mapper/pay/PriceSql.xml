<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.repository.pay.PriceRepository">
    <select id="selectPriceByStime" resultType="price" parameterType="String">
        SELECT * FROM price_tb WHERE pri_stime=#{pri_stime};
    </select>
</mapper>
